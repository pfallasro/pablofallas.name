# @vite-pwa/astro@1.2.0 declares peer astro ^1-^5. The underlying
# vite-plugin-pwa supports Astro 6 / Vite 7+ fine; the wrapper just has
# an outdated peer declaration. Remove this once a release with updated
# peers ships.
legacy-peer-deps=true
